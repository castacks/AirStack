# multi_raven_mission — 3-robot FULL raven coordination semantic search, cycled
# across four scenes (RetroNeighborhood, FireAcademy, ConstructionSite,
# AbandonedFactory).
#
# Full raven coordination (the default): semantic navigation, ray/voxel
# behaviors, target bidding. frontier_exploration is the frontier-only baseline
# variant of this.
#
# Run on OSMO (3 GPU / 24 CPU / 96Gi / 500Gi):
#   airstack osmo:mission osmo/missions/multi_raven_mission.yaml --pool <gpu-pool>
#
# Or locally:
#   python3 osmo/workspace/mission_runner.py osmo/missions/multi_raven_mission.yaml \
#     --airstack-root "$(pwd)"
#
# Schema reference: osmo/missions/README.md
name: multi_raven_mission

# Upload results (mcap bags + logs) to airlab-storage, then auto-teardown once
# the upload succeeds (needs the airlab-storage OSMO credential — airstack
# osmo:setup). Override the path with osmo:mission --nas-dest.
nas_dest: /volume4/dsta/raven-multi/

stack:
  services: [isaac-sim, robot-desktop, gcs]

env:
  NUM_ROBOTS: 3
  ISAAC_SIM_HEADLESS: "true"
  ISAAC_SIM_USE_STANDALONE: "true"
  ISAAC_SIM_SCRIPT_NAME: example_multi_drone_scene_import.py
  PLAY_SIM_ON_START: "true"

# Environments cycled across iterations. UPPERCASE keys (ENV_URL, SPAWN_POLY)
# are exported as env vars before `airstack up`; lowercase keys (search_area,
# query) are templated into the steps via {{env.<key>}}.
environments:

  - name: downtownwest
    ENV_URL: "Library/Stages/Dmytro/downtown_edited_v3_818.usd"
    # Fixed spawn (pos + orient). Re-randomize: uncomment SPAWN_POLY, drop SPAWN_CONFIGS.
    # SPAWN_POLY: "[[-3, -43], [9, -28]]"
    SPAWN_CONFIGS: '[{"x_m": -1.0, "y_m": -41.0, "orient": [0,0,0.698,0.716]}, {"x_m": 3.0, "y_m": -35.0, "orient": [0,0,0.736,0.677]}, {"x_m": 7.0, "y_m": -30.0, "orient": [0,0,0.784,0.621]}]'
    STAGE_SCALE: "1.0"
    SPAWN_HEIGHT_M: "0.2"
    RESULTS_SCENE: "DowntownWest"
    query: "fountain, food cart"
    voxel_score_threshold: 0.65
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "grass, road, tree, building, street lamp, electric cables"
    search_area:
      points:
        - {x: 150.0, y: 80.0, z: 0.0}
        - {x: 150.0, y: -80.0, z: 0.0}
        - {x: -150.0, y: -80.0, z: 0.0}
        - {x: -150.0, y: 80.0, z: 0.0}

  - name: moderncitydowntown100
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "road, building, car, parking lot, tree, traffic signal, fence"
    search_area:
      points:
        - {x: -100.0, y: -110.0, z: 0.0}
        - {x: -100.0, y: 100.0, z: 0.0}
        - {x: 80.0, y: 100.0, z: 0.0}
        - {x: 80.0, y: -110.0, z: 0.0}

  - name: retroneighborhood
    ENV_URL: "Library/Stages/RetroNeighborhood/RetroNeighborhood.stage.usd"
    # Fixed spawn (pos + orient). Re-randomize: uncomment SPAWN_POLY, drop SPAWN_CONFIGS.
    # SPAWN_POLY: "[[11, 6.5], [-13, -6.5]]"
    SPAWN_CONFIGS: '[{"x_m": 6.764, "y_m": -1.767, "orient": [0,0,-0.1193,0.9929]}, {"x_m": -0.354, "y_m": 2.251, "orient": [0,0,0.8743,0.4854]}, {"x_m": 2.939, "y_m": 5.117, "orient": [0,0,-0.7931,0.6091]}]'
    # 2nd fixed spawn (swap in): '[{"x_m": -11.323, "y_m": -3.956, "orient": [0,0,0.803,0.5959]}, {"x_m": 0.977, "y_m": 0.338, "orient": [0,0,-0.9754,0.2203]}, {"x_m": -3.943, "y_m": 2.377, "orient": [0,0,0.6611,0.7503]}]'
    # GT annotation file (annotations/<RESULTS_SCENE>.json) for finalize scoring.
    RESULTS_SCENE: "RetroNeighborhood"
    STAGE_SCALE: "0.01"
    query: "house, bus stop"
    voxel_score_threshold: 0.65
    score_threshold: -1
    voxel_min_cluster_size: 45        # not tuned — raven_nav default (35)
    background_queries: "road, grass, mountain, parking lot, tree, fence, billboard, electrical cables, container, utility pole, car garage, street light"
    search_area:
      points:
        - {x: -150.0, y: -110.0, z: 0.0}
        - {x: -150.0, y: 70.0, z: 0.0}
        - {x: 170.0, y: 70.0, z: 0.0}
        - {x: 170.0, y: -110.0, z: 0.0}
        
  - name: fireacademy
    ENV_URL: "Projects/AirStack/scenes/urban/allegheny_county_fire_academy/fire_academy.scene.usd"
    # Fixed spawn (pos + orient) — same fire-1 layout as the entry above, so this
    # query variant runs on the same spawn. Re-randomize: uncomment SPAWN_POLY.
    # SPAWN_POLY: "[[27.5, 3.8], [23.8, 14.4], [26.1, 17.5], [30, 11.8]]"
    SPAWN_CONFIGS: '[{"x_m": 27.34, "y_m": 5.359, "orient": [0,0,0.9757,0.2192]}, {"x_m": 25.982, "y_m": 12.241, "orient": [0,0,-0.2076,0.9782]}, {"x_m": 26.223, "y_m": 15.534, "orient": [0,0,-0.3285,0.9445]}]'
    # 2nd fixed spawn (swap in): '[{"x_m": 25.052, "y_m": 12.711, "orient": [0,0,0.6342,0.7731]}, {"x_m": 28.86, "y_m": 12.714, "orient": [0,0,0.5051,0.8631]}, {"x_m": 27.83, "y_m": 8.978, "orient": [0,0,0.9986,0.0538]}]'
    RESULTS_SCENE: "FireAcademy"
    STAGE_SCALE: "0.01"
    query: "red building, water tower, fuel tank"
    voxel_score_threshold: 0.65
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "road, grass, parking lot, tree, car, flood light"
    search_area:
      points:
        - {x: 13.0, y: -135.0, z: 0.0}
        - {x: 135.0, y: -13.0, z: 0.0}
        - {x: 13.0, y: 135.0, z: 0.0}
        - {x: -135.0, y: 13.0, z: 0.0}

  - name: constructionsite
    ENV_URL: "Library/Stages/ConstructionSite/ConstructionSite.stage.usd"
    # Fixed spawn (pos + orient). Re-randomize: uncomment SPAWN_POLY, drop SPAWN_CONFIGS.
    # SPAWN_POLY: "[[27.63, 2], [21, -5.8]]"
    SPAWN_CONFIGS: '[{"x_m": 22.99, "y_m": -4.679, "orient": [0,0,0.2886,0.9575]}, {"x_m": 25.203, "y_m": 0.448, "orient": [0,0,-0.2699,0.9629]}, {"x_m": 22.903, "y_m": -1.579, "orient": [0,0,0.9137,0.4063]}]'
    # 2nd fixed spawn (swap in): run-1 positions (known-good takeoff), different orientations.
    # '[{"x_m": 22.99, "y_m": -4.679, "orient": [0,0,0.7071,0.7071]}, {"x_m": 25.203, "y_m": 0.448, "orient": [0,0,1.0,0.0]}, {"x_m": 22.903, "y_m": -1.579, "orient": [0,0,-0.7071,0.7071]}]'
    RESULTS_SCENE: "ConstructionSite"
    query: "forklift, tower crane, blue tarp, orange tarp"
    voxel_score_threshold: 0.65
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "road, grass, mountain, parking lot, building, fence, container, gravel"
    search_area:
      points:
        - {x: 76.0, y: 50.0, z: 0.0}
        - {x: 76.0, y: -71.0, z: 0.0}
        - {x: -37.0, y: -67.0, z: 0.0}
        - {x: -40.0, y: 50.0, z: 0.0}

  - name: abandonedfactory
    ENV_URL: "Library/Stages/AbandonedFactory/AbandonedFactory.stage.usd"
    # Fixed spawn (pos + orient; z from SPAWN_HEIGHT_M). Re-randomize: uncomment SPAWN_POLY.
    # SPAWN_POLY: "[[16.5, -1.2], [22, 9.6]]"
    SPAWN_CONFIGS: '[{"x_m": 20.458, "y_m": 1.499, "orient": [0,0,0.1166,0.9932]}, {"x_m": 20.46, "y_m": 4.676, "orient": [0,0,-0.9881,0.1541]}, {"x_m": 20.476, "y_m": 8.299, "orient": [0,0,0.4036,0.9149]}]'
    # 2nd fixed spawn (swap in): '[{"x_m": 18.342, "y_m": 1.867, "orient": [0,0,-0.313,0.9497]}, {"x_m": 17.552, "y_m": 5.243, "orient": [0,0,-0.8323,0.5543]}, {"x_m": 20.879, "y_m": 7.149, "orient": [0,0,-0.147,0.9891]}]'
    STAGE_SCALE: "1.0"
    SPAWN_HEIGHT_M: "1.0"
    RESULTS_SCENE: "AbandonedFactory"
    query: "building, water tower, silo"
    voxel_score_threshold: 0.65          # starting default — tune per scene
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "grass, road, tree, pipe, fence"
    search_area:
      points:
        - {x: 45.0, y: 102.0, z: 0.0}
        - {x: 45.0, y: -97.0, z: 0.0}
        - {x: -109.0, y: -97.0, z: 0.0}
        - {x: -109.0, y: 102.0, z: 0.0}

  - name: militarybase
    ENV_URL: "Library/Stages/Dmytro/MilitaryBase_t_x1100_y200_z0_o_x0_y0_z90.scene.usd"
    # Fixed spawn (pos + orient). Re-randomize: uncomment SPAWN_POLY, drop SPAWN_CONFIGS.
    # SPAWN_POLY: "[[1105, 214], [1126, 240]]"
    SPAWN_CONFIGS: '[{"x_m": 1110.0, "y_m": 220.0, "orient": [0,0,-0.585,0.811]}, {"x_m": 1116.0, "y_m": 227.0, "orient": [0,0,-0.606,0.795]}, {"x_m": 1122.0, "y_m": 234.0, "orient": [0,0,-0.625,0.780]}]'
    STAGE_SCALE: "1.0"
    RESULTS_SCENE: "MilitaryBase"
    query: "guard tower, radio tower, bridge, helicopter, atv"
    voxel_score_threshold: 0.65
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "grass, road, tree, building, mountain"
    search_area:
      points:
        - {x: 1280.0, y: 320.0, z: 0.0}
        - {x: 1030.0, y: 320.0, z: 0.0}
        - {x: 1030.0, y: -150.0, z: 0.0}
        - {x: 1280.0, y: -150.0, z: 0.0}
    
  - name: shipyard
    ENV_URL: "Library/Stages/Dmytro/edit_v1_shipyard.usd"
    # Fixed spawn (pos + orient; z from SPAWN_HEIGHT_M) inside the
    # [[-54,-9],[-72,-24]] rect. Re-randomize: uncomment SPAWN_POLY, drop SPAWN_CONFIGS.
    # SPAWN_POLY: "[[-54, -9], [-72, -24]]"
    SPAWN_CONFIGS: '[{"x_m": -66.066, "y_m": -11.867, "orient": [0,0,-0.9089,0.417]}, {"x_m": -68.796, "y_m": -14.581, "orient": [0,0,-0.6509,0.7591]}, {"x_m": -61.737, "y_m": -14.407, "orient": [0,0,0.2744,0.9616]}]'
    STAGE_SCALE: "1.0"
    SPAWN_HEIGHT_M: "2.0"
    RESULTS_SCENE: "Shipyard"
    query: "ship, white silo, crane"
    voxel_score_threshold: 0.65 
    score_threshold: -1
    voxel_min_cluster_size: -1
    background_queries: "grass, road, tree, warehouse, pipe, fence"
    search_area:
      points:
        - {x: -150.0, y: 150.0, z: 0.0}
        - {x: 150.0, y: 150.0, z: 0.0}
        - {x: 150.0, y: -150.0, z: 0.0}
        - {x: -150.0, y: -150.0, z: 0.0}

  

# Send goals through the GCS action_relay — the exact path Foxglove uses.
command_route: gcs

# grouped: each environment runs `iterations` times in a row (per-environment
# count; total runs = iterations * 8 environments). Fixed spawns per SPAWN_CONFIGS.
environment_order: grouped
iterations: 1

# Redo an iteration (clean down→up→fly→down) if a step up to and including
# semantic_search fails — i.e. the precondition steps fail, or semantic_search
# never gets feedback after its own retries. Up to 3 attempts per iteration.
iteration_attempts: 3

ready:
  timeout_s: 600

record:
  enabled: true
  scope: gcs
  required: true
  topics:
    - /tf
    - /tf_static
    - /{robot}/odometry_conversion/odometry
    - /gcs/robot_markers
    - /gcs/payload/{robot}/filtered_rays
    - /gcs/payload/{robot}/filtered_frontiers
    - /gcs/payload/{robot}/explored_area_coverage
    - /gcs/payload/{robot}/confirmed_targets
    - /gcs/payload/{robot}/auction_table
    - /gcs/payload/{robot}/voxel_rgb
    - /gcs/sim_ground
    - /gcs/annotations/bboxes
    - /rayfronts_debug/{robot}/voxels_sim/all
    - /rayfronts_debug/{robot}/voxel_rgb

on_step_failure: abort_iteration

steps:
  - run:
      container: robot_1
      cmd: python3 -c "import torch; torch.hub.load('NVlabs/RADIO', 'radio_model', version='c-radio_v3-b', skip_validation=True, adaptor_names=['siglip2','sam'])"
      timeout_s: 900
      attempts: 5
      retry_delay_s: 15

  - action:
      task: takeoff
      goal: {target_altitude_m: 12.0, velocity_m_s: 1.0}
      timeout_s: 180

  - wait: 5

  # Run budget = 15 min OR 80% explored, whichever first (cancel_on_timeout lets
  # semantic_search_task run its compile + metrics finalize on the deadline).
  # pass_on_feedback: the iteration only needs the search to RUN — pass as soon
  # as feedback is seen, regardless of outcome; redo the iteration only if no
  # feedback arrives after the retries.
  - action:
      task: semantic_search
      timeout_s: 900
      cancel_on_timeout: true
      pass_on_feedback: true
      goal:
        query: "{{env.query}}"
        background_queries: "{{env.background_queries}}"
        search_area: "{{env.search_area}}"
        min_altitude_agl: 5.0
        max_altitude_agl: 20.0
        min_flight_speed: 0.5
        max_flight_speed: 1.0
        confidence_threshold: 0.95
        # Per-scene voxel similarity gate (see each environment block above).
        voxel_score_threshold: "{{env.voxel_score_threshold}}"
        # Per-scene ray score gate + voxel cluster size (-1 on an entry = raven_nav default).
        score_threshold: "{{env.score_threshold}}"
        voxel_min_cluster_size: "{{env.voxel_min_cluster_size}}"
        # Consensus bundle length (head executed, tail reserved + replanned).
        bundle_len: 3

