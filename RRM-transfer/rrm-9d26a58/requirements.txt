# RRM-1 core. Deliberately minimal: the loop must run with no GPU and no models
# so it can serve as the CI target (docs/benchmarks.md §5).
pydantic>=2.0

# Phase 2+ (A10G only) — uncomment on the instance:
# torch>=2.4
# transformers>=4.45
# vllm>=0.11.0          # guided_json / xgrammar for the reasoner
# lerobot               # GR00T + SmolVLA harness
# huggingface_hub[cli]
