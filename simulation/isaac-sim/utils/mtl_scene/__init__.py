"""Pure-Python helpers for the MTL search scene (search_mission_scene.py).

Nothing here imports omni/pxr/pegasus, so the configuration resolution and the
gimbal kinematics are unit-tested without an Isaac install
(tests/colcon_unit_test_packages.yaml -> sim: mtl_scene).
"""
