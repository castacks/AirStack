# Procedural city spec — consumed by utils/scene_generator.py
#
# Usage:
#   bake:    python utils/scene_generator.py --config <this file> --output out.usd
#   runtime: generate_scene_on_stage(stage, <this path>, scene_scale_factor=ssf)
#
# Units: meters. +X = East, +Y = North, +Z = Up. Region centered on origin.
#
# PIPELINE (generation order):
#   0. resolve   — relative usds.* paths prefixed with asset_root; per-USD
#                  footprint measured from bbox (measure_usds) else fallback_sizes
#   1. roads     — metric BSP subdivision into blocks + road corridors (roads.*)
#                  1 asphalt plane for the whole region; 1 grass plane per block;
#                  yellow/white lane-marking dashes per corridor
#   2. ground    — per block: brick sidewalk ring; packed blocks paved wall-to-wall
#   3. buildings — each block packed out to the sidewalk (guillotine packing,
#                  packing.*) drawing from intact/damaged/destroyed pools by
#                  fate weight (disaster.damaged_fraction / destroyed_fraction),
#                  each with its own footprint; park_block_chance blocks stay
#                  grass; destroyed ruins get debris piles hugging the base +
#                  settled fragments, and sometimes lean (tilt + sink) with
#                  rubble on the caved side (disaster.debris); oversized
#                  buildings get anchor blocks; placeholders fill gaps when enabled
#   3b. parks    — park blocks get a meandering trail (tiles.trail) with
#                  park-tagged benches/lamps/trash cans along it (parks.*);
#                  a playground cluster off the trail (playground_* knobs,
#                  usds.play_structures); rocks + plants scattered on park
#                  grass (usds.rocks, plants.*); trees spread evenly at
#                  parks.tree_density_per_100m2
#   4. detail    — humans on sidewalks/trails/grass (humans.*); bus stops,
#                  streetlights, benches, planters (street trees + plant
#                  boxes), fire hydrants, trash cans along block frontages,
#                  each inset onto the sidewalk (frontage.curb_margin_m;
#                  shared occupancy grid — categories never stack); traffic
#                  lights at NS×EW corridor intersections (traffic_lights.*)
#   5. cars      — cars parked along road corridors, right-hand traffic (cars.*)
#
# disaster.* holds ALL disaster settings: building fates + debris (step 3)
# and the steps 4-5 aftermath (topples, leaning traffic lights, wind-scattered
# trash cans, prone humans, strewn wrecks). preset_file (below) deep-merges a
# preset YAML over this config — presets can override any setting.
#
# exclusions apply to clutter only (houses, trees, streetlights, traffic
# lights, hydrants, trash cans, humans, cars); ground tiles and roads still
# cover excluded areas.

seed: 42                    # int — same seed => identical city

# ---- presets ------------------------------------------------------
presets_path: "simulation/isaac-sim/config/scene_generator_presets"
# preset_file: None
# preset_file: "untouched.yaml"
# preset_file: "light.yaml"
preset_file: "moderate.yaml"
# preset_file: "severe.yaml"
# preset_file: "total.yaml"



# ---- clutter keep-out zones ------------------------------------------------------
# circle {center: [x, y], radius: r} or rect {bounds: [[xmin, ymin], [xmax, ymax]]}
exclusions:
  - center: [0.0, 0.0]
    radius: 10.0             # keep the takeoff pad clear

# ---- step 0: asset resolution ------------------------------------------------
asset_root: "omniverse://airlab-nucleus.andrew.cmu.edu:443/Library/Stages/"
                            # prefix for relative usds.* paths; URLs / absolute paths pass through
asset_scale: 1           # global unit scale — default fits Dmytro assets (authored in meters);
                            # RetroNeighborhood assets override to 0.01 (authored in cm) below
measure_usds: true          # true = measure footprints from USD bboxes (needs asset access);
                            # per-category fallback_sizes used when a measurement fails

# ---- step 1: road network / block layout -----------------------------------
layout:
  region_m: [400, 400]      # [x, y] m — total city extent
  min_block_m: 30           # m — never split a block smaller than this
  max_block_m: 70           # m — always split larger (auto-raised for oversized buildings)
  split_jitter: 0.25        # 0..1 — 0 = split at center, 1 = anywhere in valid range
  tile_overlap: 1.02        # ground-tile seam overlap; tiles are stretched to exactly
                            # cover their grid step, this inflates them a hair further
                            # so neighbors overlap instead of meeting at a float-exact seam

# ---- step 3: block packing ------------------------------------------------------
packing:
  building_gap_m: 2.5       # m — clearance between adjacent buildings in a block
  park_block_chance: 0.12   # 0..1 — fraction of blocks left as grassy parks
                            # (where trees/plants/humans end up)
  min_parks: 2              # guaranteed minimum park count — extra blocks are
                            # promoted to parks when the chance rolls land short
  max_parks: 3            # optional cap; set min = max to pin an exact count

  placeholders:             # filler prisms for gaps no library building fits
    enabled: false
    min_footprint_m: 5.0    # m — gaps thinner than this stay pavement
    max_footprint_m: 20.0   # m — bigger gaps split into several varied prisms
    height_m: [8, 30]       # [min, max] m — random prism height range

# ---- step 4: detail ------------------------------------------------------------
frontage:
  curb_margin_m: 0.3        # m — gap between a street prop's mesh edge and the curb
                            # line; every frontage prop is inset by half its short
                            # footprint axis plus this, so nothing pokes into the road

trees:
  house_margin_m: 1.5       # m — keep-out ring around trail tiles (park scatter);
                            # street trees are potted — see planters.tree_spacing_m

plants:
  per_block: [4, 12]        # [min, max] — plant clumps per PARK block (park grass only);
                            # sidewalk plants are potted — see planters.plant_spacing_m
  min_separation_m: 1.0     # m — minimum plant-to-plant distance
  house_margin_m: 1.5       # m — keep-out ring around trail tiles

planters:
  tree_spacing_m: 30.0      # m — street trees (in planters tagged "tree") along frontage
  plant_spacing_m: 40.0     # m — plant boxes along frontage
  plant_slot_m: 0.7         # m — planter length per plant clump (1-3 clumps per box)
  soil_level_frac: 0.75     # 0..1 — greenery base sits at this fraction of the
                            # planter's height (tall bush planters ≠ ground level)

bus_stops:
  spacing_m: 130.0          # m — shelter spacing along frontages (≈ one per block or two)

streetlights:
  spacing_m: 18.0           # m — spacing along block frontages

benches:
  spacing_m: 30.0           # m — spacing along block frontages (uses usds.benches
                            # entries tagged "sidewalk")

fire_hydrants:
  spacing_m: 55.0           # m — spacing along block frontages (one per ~half block)

trash_cans:
  spacing_m: 25.0           # m — spacing along block frontages

traffic_lights:
  intersection_chance: 0.9  # 0..1 — chance a road intersection gets a light
  corner_margin_m: 1.0      # m — pole distance past the road corner onto the
                            # sidewalk; pole faces the intersection center

humans:
  per_block: [0, 3]         # [min, max] — idlers per block on open grass (parks mostly)
  sidewalk_spacing_m: 45.0  # m — pedestrians along sidewalks, walking with the
                            # street (0 disables)
  trail_spacing_m: 30.0     # m — walkers along park trails (0 disables)
  min_separation_m: 2.0     # m — minimum person-to-person distance
  house_margin_m: 1.0       # m — keep-out ring around building lots

# ---- park blocks (see packing.park_block_chance) ---------------------------------
# Each park gets a meandering trail (tiles.trail) edge to edge across its longer
# axis, with park-tagged furniture (tags: ["park"] in usds entries) along the
# sides — benches face the trail — and rocks scattered on the grass.
parks:
  trail_margin_m: 4.0       # m — meander lateral clearance from the park edge
  trail_waypoints: [2, 4]   # [min, max] — interior meander waypoints
  bench_spacing_m: 14.0     # m — along-trail spacing of park benches
  streetlight_spacing_m: 22.0 # m — along-trail spacing of park lamps
  trash_can_spacing_m: 30.0 # m — along-trail spacing of park trash cans
  furniture_offset_m: 0.8   # m — gap between the trail edge and furniture
  rocks_per_park: [3, 8]    # [min, max] — rocks scattered per park
  rock_min_separation_m: 2.5 # m — minimum rock-to-rock distance
  tree_density_per_100m2: 0.8 # park trees per 100 m² of park grass — evenly
                            # spread (best-candidate sampling), off the trail
  tree_min_separation_m: 4.0 # m — minimum park-tree-to-tree distance
  playground_chance: 0.6    # 0..1 — chance a (big-enough) park gets a playground
  playground_structures: [2, 3] # [min, max] — play structures per playground
  playground_spacing_m: 5.0 # m — structure distance from the playground center
  playground_margin_m: 2.0  # m — keep-out ring around structures (blocks
                            # trees/rocks/humans from spawning inside)

# ---- step 5: cars ----------------------------------------------------------------
cars:
  density: 0.15             # 0..1 — fraction of straight road cells that get a car
  lane_offset_frac: 0.3     # 0..1 — lateral offset from road centerline; 0 = centerline,
                            # 1 = road edge; 0.5 = middle of the right-hand lane.
                            # Travel direction is random per car; offset is always to
                            # that direction's right (right-hand traffic)
  yaw_jitter_deg: 4.0       # deg — random heading wobble per car

# ---- disaster ---------------------------------------------------------------
# Everything disaster-related lives here — building fates (step 3), debris
# fields, and the step 4-5 aftermath (topples, wrecks, casualties). The
# preset files (see presets_path at the top) override this whole block.
disaster:
  # Building fates: packing draws from intact / damaged / destroyed pools by
  # these pick weights (intact gets the remainder).
  damaged_fraction: 0.3      # 0..1 — pick weight for usds.buildings.damaged art
                             # (tilted + sunk intact stand-in when the pool is empty)
  destroyed_fraction: 0.4    # 0..1 — pick weight for usds.buildings.destroyed art,
                             # with a debris field around it (debris below)

  debris:                    # scattered around each destroyed building
    piles_per_building: [2, 4]   # [min, max] — rubble piles hugging the ruin
                                 # (overlap with building/road is fine)
    pile_max_offset_m: 3.0       # m — max pile offset beyond the footprint edge
    pieces_per_building: [10, 20] # [min, max] — smaller fragments just past the
                                 # footprint; physics-settled like the toppled cars
    pieces_scatter_m: 6.0        # m — max fragment distance beyond the footprint
    tilt_chance: 0.35            # 0..1 — destroyed ruins also lean: slight
                                 # tilt + sink, rubble piled on the caved-in side
    tilt_deg: [2, 6]             # [min, max] deg — lean angle (roll and pitch)
    sink_m: [0.4, 1.2]           # [min, max] m — how far a leaning ruin sinks
    lean_piles: [2, 3]           # [min, max] — extra piles at the overhanging base

  # Aftermath (steps 4-5) — tornado-style street damage.
  streetlights_toppled_fraction: 0.3    # 0..1 — poles snapped flat, random heading
  traffic_lights_toppled_fraction: 0.25 # 0..1 — poles snapped flat at the base
  traffic_lights_leaning_fraction: 0.3  # 0..1 — of still-standing poles: bent/leaning
  traffic_lights_lean_deg: [8, 30]      # [min, max] deg — lean angle range
  trash_cans_toppled_fraction: 0.6      # 0..1 — light + unanchored: most get tipped
  trash_cans_scatter_m: 4.0             # m — max wind displacement of tipped cans
  cars_toppled_fraction: 0.2            # 0..1 — lane cars flipped on side/roof
  cars_strewn: [4, 10]                  # [min, max] — extra wrecks tossed onto open ground
  strewn_topple_fraction: 0.7           # 0..1 — strewn cars flipped (rest stay upright)
  strewn_margin_m: 1.5                  # m — keep strewn cars off building footprints
  humans_prone_fraction: 0.35           # 0..1 — pedestrians/idlers lying flat where
                                        # they stood (casualties / taking cover)
  humans_strewn: [2, 6]                 # [min, max] — extra casualties on road asphalt

# ---- per-family yaw offsets (deg), added to auto-computed rotations -----------
# (for a single misaligned asset, prefer a per-USD yaw-offset in its usds entry)
orientation:
  house_front:   0.0        # building front facing offset
  car_front:     0.0       # 0 = car art faces +X (its direction of travel)
  traffic_light_front: 0.0  # 0 = art faces +X (auto-yawed toward intersection center)

# ---- road network configuration -----------------------------------------------
roads:
  lane_width_m: 3.5          # m — width of a single traffic lane (realistic urban: 3–4m)
  main_road_lanes: 4         # total lanes for main roads (2 per direction)
  secondary_road_lanes: 2    # total lanes for secondary roads (1 per direction)
  border_lanes: 2            # lanes for the outer ring road
  main_road_chance: 0.25     # 0..1 — probability a BSP split becomes a main (wide) road
  asphalt_uv_scale_m: 4.0   # m per UV unit for asphalt tiling
  grass_uv_scale_m: 3.0     # m per UV unit for grass tiling
  lane_lines:
    dash_length_m: 3.0       # m — length of each white/yellow dash
    dash_gap_m: 3.0          # m — gap between dashes
    dash_width_m: 0.15       # m — width of a single dash stripe
    center_line_width_m: 0.20 # m — lateral separation between the two yellow centre lines

# ---- footprints [x, y(, z)] m — used when measure_usds is false or a measure fails
fallback_sizes:
  concrete:      [4.0, 4.0, 0.1]   # thin slab — z feeds the walking-surface height
  sidewalk:      [2.0, 2.0, 0.1]   # thin slab — z feeds the walking-surface height
  trail:         [4.0, 3.0, 0.1]
  rock:          [1.5, 1.5]
  house:         [48.0, 48.0]
  tree:          [3.0, 3.0]
  plant:         [1.0, 1.0]
  streetlight:   [1.0, 1.0]
  bench:         [2.0, 0.6, 0.9]
  car:           [4.5, 2.0, 1.5]
  fire_hydrant:  [0.5, 0.5, 0.9]
  trash_can:     [0.8, 0.8, 1.2]
  traffic_light: [1.0, 1.0, 6.0]
  human:         [1.2, 0.35, 1.8]  # A-pose bbox; y = body depth (prone lift)
  bus_stop:      [4.0, 1.5, 2.5]
  planter:       [1.0, 1.0, 0.5]
  play_structure: [4.0, 2.0, 2.5]
  debris_pile:   [3.0, 3.0, 1.5]
  debris:        [1.5, 1.5, 0.8]

# ---- asset library --------------------------------------------------------------
# Entry forms:  "path.usd"                                (global asset_scale, Z-up)
#           or  {usd: "path.usd", scale: 1.0, axis-up: "Y", yaw-offset: 90}
# Per-asset overrides: scale (unit conversion), axis-up ("Y" for Y-up-authored
# art, fixed via +90° roll), yaw-offset (deg — for art whose front isn't +X;
# added to every computed yaw, e.g. a streetlight arm that points +Y needs -90).
# Relative paths are resolved against asset_root.

# sky: equirect HDRI (.exr/.hdr) applied to the dome light, OR a stage .usd
# whose root sky/environment prims (outside its defaultPrim) get borrowed.
# RetroNeighborhood's /Environment holds a dynamic-sky rig + dome light and
# no geometry, so borrowing it costs almost nothing at load time.
sky: "RetroNeighborhood/RetroNeighborhood.stage.usd"

usds:
  tiles:
    concrete: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_GroundModule01.usd"
    sidewalk: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_GroundModule01.usd"
             # brick tiles used for sidewalk rings around each block; Dmytro asset, uses global default
    trail:
      usd: "RetroNeighborhood/Props/SM_DriveWay_01.prop.usd"
      scale: 0.01   # RetroNeighborhood authored in cm; override the global default

  materials:
    grass: "Muyang/StandaloneMaterials/Grass/MI_Grass.usd"
    asphalt: "Muyang/StandaloneMaterials/Asphalt/MI_Asphalt.usd"

  buildings:
    # houses are exempt from the Dmytro/RetroNeighborhood scale flip-flop below —
    # each entry keeps its original effective footprint, expressed against the
    # new global default (1.0)
    intact:
      # - usd: "RetroNeighborhood/Props/SM_House_01.prop.usd"
      #   scale: 0.01   # RetroNeighborhood authored in cm; unchanged effective footprint
      - "Muyang/ModernCityEnvironment/Collected_Building01/SM_MERGED_BP_MBuilding01.usd"
        # authored in meters; already matches the new global default (1.0)
      - usd: "Muyang/ModernCityEnvironment/Collected_Building02/SM_MERGED_BP_MBuilding02.usd"
        scale: 0.01   # unchanged effective footprint (was the old global default)
      - usd: "Muyang/ModernCityEnvironment/Collected_Building03/SM_MERGED_BP_MBuilding03.usd"
        scale: 0.01   # unchanged effective footprint (was the old global default)
      - usd: "Muyang/ModernCityEnvironment/Collected_Building05/SM_MERGED_BP_MBuilding05.usd"
        scale: 0.01   # unchanged effective footprint (was the old global default)
      - usd: "Muyang/DownTown/Assets/BG_Building_A.usd"
        scale: 0.01   
      - usd: "Muyang/DownTown/Assets/BG_Building_B.usd"
        scale: 0.01   
      - usd: "Muyang/DownTown/Assets/BG_Building_C.usd"
        scale: 0.01   
      - usd: "Muyang/DownTown/Assets/BG_Building_D.usd"
        scale: 0.01   
      - usd: "Muyang/DownTown/Assets/BG_Building_E.usd"
        scale: 0.01   
      - usd: "Muyang/DownTown/Assets/BG_Building_F.usd"
        scale: 0.01   

    damaged:
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_4.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_4_variation_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_5.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_9.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_10.usd"
        scale: 0.01

    destroyed:
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_2.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_5_variation_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_6.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_7.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_8.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_9_variation_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_10_variation_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_11_variation_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/destroyed_building_12.usd"
        scale: 0.01

  debris:
    pieces:
      - usd: "Muyang/DestroyedBuildings/Assets/debris_2.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_3.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_4.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_5.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_6.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_7.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_8.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_9.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_10.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_11.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Blok_1_LODs.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Blok_2_LODs.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Blok_3_LODs.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Blok_4_LODs.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_01.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_02.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_03.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_04.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_05.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_06.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_red_07.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_white_01.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_white_02.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_white_04.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_white_05.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Brick_white_06.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_01.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_02.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_03.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_04.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_05.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_06.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_07.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_08.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_09.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_10.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_11.usd"
        scale: 0.01
      - usd: "Muyang/DebrisPack/Assets/Debris_12.usd"
        scale: 0.01
    piles:
      - usd: "Muyang/DestroyedBuildings/Assets/debris_1.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_12.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_14.usd"
        scale: 0.01
      - usd: "Muyang/DestroyedBuildings/Assets/debris_15.usd"
        scale: 0.01

  play_structures:
    - usd: "Muyang/CivilianArea/Assets/SM_PlaygroundSlide_01.usd"
      scale: 0.01
    - usd: "Muyang/CivilianArea/Assets/SM_PlaygroundSwings_01.usd"
      scale: 0.01
    - usd: "Muyang/CivilianArea/Assets/SM_PlaygroundSwings_02.usd"
      scale: 0.01

  trees:
    # - usd: "RetroNeighborhood/Props/SM_Tree_01.prop.usd"
    #   scale: 0.01
    - usd: "RetroNeighborhood/Props/SM_Tree_02.prop.usd"
      scale: 0.01
    - usd: "RetroNeighborhood/Props/SM_Tree_03.prop.usd"
      scale: 0.01
    - usd: "RetroNeighborhood/Props/SM_Tree_04.prop.usd"
      scale: 0.01
    - usd: "RetroNeighborhood/Props/SM_Tree_05.prop.usd"
      scale: 0.01
    - usd: "RetroNeighborhood/Props/SM_Tree_06.prop.usd"
      scale: 0.01
    - usd: "RetroNeighborhood/Props/SM_Tree_07.prop.usd"
      scale: 0.01
    # - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Tree1.usd"
    # - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Tree2.usd"
    # - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Tree3.usd"
    - usd: "CityPark/Props/SM_Tree_01.prop.usd"
      scale: 0.01
    - usd: "CityPark/Props/SM_Tree_03.prop.usd"
      scale: 0.01
    - usd: "CityPark/Props/SM_Tree_04.prop.usd"
      scale: 0.01
    - usd: "CityPark/Props/SM_Tree_05.prop.usd"
      scale: 0.01
    - usd: "CityPark/Props/SM_Tree_08.prop.usd"
      scale: 0.01
    # - usd: "Muyang/DownTown/Assets/Tree_A.usd"
    #   scale: 0.01
    # - usd: "Muyang/DownTown/Assets/Tree_B.usd"
    #   scale: 0.01
    # - usd: "Muyang/DownTown/Assets/Tree_C.usd"
    #   scale: 0.01
    - usd: "CityPark/Props/SM_Stump.prop.usd"
      scale: 0.01
      tags: ["stump"]
    - usd: "CityPark/Props/SM_Stump_02.prop.usd"
      scale: 0.01
      tags: ["stump"]
    - usd: "CityPark/Props/SM_Stump_03.prop.usd"
      scale: 0.01
      tags: ["stump"]

  plants:
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Grass.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Grass_Flowers.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Grass_Flowers1.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Grass_Flowers2.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bush_1.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bush_2.usd"
    - usd: "Muyang/DownTown/Assets/Plant_Red.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Plant_Purple.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Plant_Pink.usd"
      scale: 0.01

  rocks:
    - usd: "CityPark/Props/SM_Stone_04.prop.usd"
      scale: 0.01
    - usd: "CityPark/Props/SM_Stone_05.prop.usd"
      scale: 0.01
    # - usd: "CityPark/Props/SM_Stone_06.prop.usd"
    #   scale: 0.01
    - usd: "CityPark/Props/SM_Stone_08.prop.usd"
      scale: 0.01
    - usd: "CityPark/Props/SM_Stone_09.prop.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Rock_A.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Rock_B.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Rock_C.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Rock_D.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Rock_E.usd"
      scale: 0.01
  
  planters:
    - usd: "Muyang/DownTown/Assets/Planter_A.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/Planter_B.usd"
      scale: 0.01
    - usd: "Muyang/DownTown/Assets/PlanterLarge_A.usd"
      scale: 0.01
      tags: ["tree"]

  streetlights:
    - usd: "RetroNeighborhood/Props/SM_StreetLight.prop.usd"
      scale: 0.01
      yaw-offset: 90.0
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Lamp_002.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Lamp_005.usd"
    - usd: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Lamp_006.usd"
      tags: ["park"]
  
  bus_stops:
    - usd: "Muyang/DownTown/Assets/BusStop.usd"
      scale: 0.01
      yaw-offset: 180.0

  trash_cans:
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_MTrashCan.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bin_003.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bin_003_01.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bin_003_02.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bin_004.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bin_006.usd"
    - usd: "CityPark/Props/SM_Small_Trash_can.prop.usd"
      scale: 0.01
      tags: ["park"]
    - usd: "Muyang/DownTown/Assets/garbageCan.usd"
      scale: 0.01
  
  traffic_lights:
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Traffic_Lamp_001.usd"
    - "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Traffic_Lamp_002.usd"

  fire_hydrants:
    - usd: "RetroNeighborhood/Props/SM_FireHydrant.prop.usd"
      scale: 0.01

  benches:
    - usd: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bench01.usd"
      yaw-offset: -90.0
      tags: ["sidewalk"]
    - usd: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bench02.usd"
      yaw-offset: -90.0
      tags: ["sidewalk"]
    - usd: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SM_Bench03.usd"
      yaw-offset: -90.0
      tags: ["sidewalk"]
    - usd: "Muyang/DownTown/Assets/Bench.usd"
      scale: 0.01
      yaw-offset: 0.0 # TODO?
      tags: ["sidewalk", "park"]
    - usd: "CityPark/Props/SM_Log_shop.prop.usd"
      scale: 0.01
      yaw-offset: 90.0
      tags: ["park"]
    - usd: "CityPark/Props/SM_Log_shop_02.prop.usd"
      scale: 0.01
      yaw-offset: 90.0
      tags: ["park"]

  cars:
    - usd: "RetroNeighborhood/130.usdz"
      scale: 0.01
      axis-up: "Y"           # authored Y-up; fixed to Z-up for Isaac Sim
      yaw-offset: 90.0
    - usd: "RetroNeighborhood/FREE_GMC_Motorhome_reimagined_low_poly.usdz"
      scale: 0.01
      axis-up: "Y"
      yaw-offset: 90.0
    # - usd: "RetroNeighborhood/FREE_Merc_Hovercar.usdz"
    #   scale: 0.01
    #   axis-up: "Y"
    - usd: "RetroNeighborhood/Nissan_Fairlady_Z_S30240Z_1978.usdz"
      scale: 0.01
      axis-up: "Y"
      yaw-offset: 90.0
    # - usd: "RetroNeighborhood/Old_Rusty_Car.usdz"
    #   scale: 0.01
    #   axis-up: "Y"
    # - usd: "RetroNeighborhood/ZIS-101A_Sport_1938.usdz"
    #   scale: 0.01
    #   axis-up: "Y"
    - usd: "Dmytro/Assets/Game/ModernCityEnvironment01/Meshes/SK_ConstructionTruck01FullyRigged.usd"
    - usd: "Muyang/DownTown/Assets/Vehicle_A.usd"
      scale: 0.01
      yaw-offset: 90.0
    - usd: "Muyang/DownTown/Assets/Vehicle_Police.usd"
      scale: 0.01
      yaw-offset: 90.0
    - usd: "Muyang/DownTown/Assets/Vehicle_Taxi.usd"
      scale: 0.01
      yaw-offset: 90.0


  # RenderPeople rigged humans: cm-authored (scale 0.01), art faces -Y
  # (yaw-offset 90). UsdSkel rigs with no animation clip — the generator
  # authors static idle/walk poses onto them (see _HUMAN_POSES).
  humans:
    - {usd: "Muyang/People/rp_carla_rigged_001_ue4.usd", scale: 0.01, yaw-offset: 90}
    - {usd: "Muyang/People/rp_claudia_rigged_002_ue4.usd", scale: 0.01, yaw-offset: 90}
    - {usd: "Muyang/People/rp_eric_rigged_001_ue4.usd", scale: 0.01, yaw-offset: 90}
    - {usd: "Muyang/People/rp_manuel_rigged_001_ue4.usd", scale: 0.01, yaw-offset: 90}
    - {usd: "Muyang/People/rp_nathan_rigged_003_ue4.usd", scale: 0.01, yaw-offset: 90}
    - {usd: "Muyang/People/rp_sophia_rigged_003_ue4.usd", scale: 0.01, yaw-offset: 90}







