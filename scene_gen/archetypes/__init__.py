"""Stage A — the archetype library. See SPEC.md, "Pipeline".

Bake once, exhaustively, layout-independently: every displaceable asset TYPE
crossed with every damage LEVEL, exported as a self-contained USD. Stage B then
assembles a scene by REFERENCE, which is what removes per-scene physics.
"""
