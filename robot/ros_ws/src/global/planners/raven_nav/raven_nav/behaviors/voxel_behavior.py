# Voxel-based behavior - placeholder for future implementation.
# Enable once voxel behavior strategy is decided.

# class VoxelBehavior:
#     def __init__(self, get_clock):
#         self.get_clock = get_clock
#         self.name = 'Voxel-based'
#
#     def condition_check(self, voxels, voxel_scores, target_labels, target_objects):
#         return False
#
#     def execute(self, voxels, voxel_scores, target_labels, target_objects,
#                 cur_pose_np, waypoint_locked, target_waypoint, target_waypoint2,
#                 publisher_dict):
#         return waypoint_locked, target_waypoint, target_waypoint2
