[develop]
script_dir=$base/lib/lidar_point_cloud_filter
[install]
install_scripts=$base/lib/lidar_point_cloud_filter

[tool:pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Hermetic unit tests (no ROS stack required)
